version: "3.8"

services:
  rabbitmq:
    image: "rabbitmq:management"
    ports:
      - "5672:5672"
      - "15672:15672"
  redis:
    image: "redis:alpine"
    hostname: redis
    ports:
      - "6379:6379"
